[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "djenmonitor"
version = "0.1.0"
dependencies = []

[tool.vercel]
entrypoint = "api.v1.comunicacao:handler"
